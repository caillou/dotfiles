# Interactive-only config. Environment lives in ~/.zprofile (read by login shells,
# including Claude Code's tool shell).
[[ -o login ]] || source ~/.zprofile

if command -v wt >/dev/null 2>&1; then eval "$(command wt config shell init zsh)"; fi
